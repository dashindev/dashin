{
  "license": "Apache-2.0",
  "private": true,
  "author": "BunRed Team",
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "tsc:watch": "lerna run --parallel tsc:watch",
    "tsc:clean": "lerna run --parallel tsc:clean",
    "tsc:build": "lerna run ttsc:build && lerna run tsc:build",
    "dev": "cd packages/bunadmin && yarn start"
  },
  "devDependencies": {
    "lerna": "^5.1.4"
  }
}