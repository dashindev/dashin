import { notice, ConfirmDialog } from "@dashin-dev/dashin"

# Dialog

## ConfirmDialog

<ConfirmDialog
  doFunc={async () => {
    await notice({
      title: "Confirmed",
      content: "This is a ConfirmDialog test"
    })
  }}
  openModal={true}
  title="Demo Dialog Title"
  msg="This is an example of content, Click `CONFIRM` will active a Notice"
/>

```tsx
import { notice } from "@dashin-dev/dashin"
//...
;<ConfirmDialog
  doFunc={async () => {
    await notice({
      title: "Confirmed",
      content: "This is a ConfirmDialog test"
    })
  }}
  openModal={true}
  title="Demo Dialog Title"
  msg="This is an example of content, Click `CONFIRM` will active a Notice"
/>
```

## Props

```ts
interface Interface {
  openModal: number
  title?: string | ReactElement
  msg?: string | ReactElement
  content?: ReactElement
  doFunc: () => void
  disagree?: string | ReactElement
  agree?: string | ReactElement
}
```
